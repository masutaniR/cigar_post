<div class="container">
  <div class="row mt-2">
    <div class="col-md-7 order-md-2 mx-auto">
      <h4><%= @user.name %>さんのいいね一覧</h4>
      <% if @posts.count == 0 %>
        <p class="mt-4 text-muted">まだいいねした投稿はありません</p>
      <% else %>
        <div class="col-12 post-container rounded">
          <%= render 'public/posts/index', posts: @posts %>
        </div>
      <% end %>

      <div class="paginate mt-3">
        <%= paginate @posts %>
      </div>
    </div>

    <div class="col-md-4 order-md-1 sidebar">
      <%= render 'public/users/info', user: @user %>
    </div>
  </div>
</div>